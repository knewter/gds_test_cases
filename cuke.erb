<%= "Scenario: " +  params["Test"] %>
<%= "  Given an applicant loaded from file " + '"' + params["Test"] + ".xml" + '"' %>
<%= "  When the applicant is sent to processes_application" %>
<%= '  Then the result should have key "Status" set to "' + params["ApplicationStatus"] + '"' %>

